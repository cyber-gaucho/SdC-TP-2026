ARCH := arm
CROSS_COMPILE := arm-linux-gnueabihf-

KDIR := ../../kernel-src-6.12.75

obj-m += cdd_gpio.o

all:
	make -C $(KDIR) \
		M=$(PWD) \
		ARCH=$(ARCH) \
		CROSS_COMPILE=$(CROSS_COMPILE) \
		modules

clean:
	make -C $(KDIR) \
		M=$(PWD) \
		ARCH=$(ARCH) \
		CROSS_COMPILE=$(CROSS_COMPILE) \
		clean
